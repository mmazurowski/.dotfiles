# lazygit on macOS defaults to ~/Library/Application Support/lazygit. Point it at
# the stowed config instead, so its theme is versioned with everything else.
export LG_CONFIG_FILE="$HOME/.config/lazygit/config.yml"
